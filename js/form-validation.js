/*
function validateForm() {
	// Checks to see if first name is filled out
    var firstName = document.forms["exit-survey"]["first_name"].value;
    if (firstName == "") {
        var d = document.getElementById("first-name-label");
        d.className += " invalid";
        return false;
    }
    // Checks to see if last name is filled out
    var lastName = document.forms["exit-survey"]["first_name"].value;
    if (lastName == "") {
        var d = document.getElementById("last-name-label");
        d.className += " invalid";
        return false;
    }
}
*/